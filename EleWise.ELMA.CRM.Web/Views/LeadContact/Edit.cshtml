@model EleWise.ELMA.CRM.Web.Models.LeadContactModel
@using EleWise.ELMA;
@using EleWise.ELMA.CRM.Web

@{
    Html.Header(SR.T("Контакт - {0}", Model.Entity.Name));
}

@(Html.Toolbar()
    .Group("toolbar-group-1")
        .ToolbarSubmit(SR.Save, "#save.svg", "toolbar-action-Save")
        .ToolbarLink(SR.Cancel, "#close_outline.svg", "javascript:history.back(-1);", "toolbar-action-Cancel"))

@Html.Partial("Editors/LeadContactEdit")