@model IContractor
@using EleWise.ELMA.CRM.Models
@using EleWise.ELMA.CRM.Web

@if (Model != null)
{
    @Html.Action("GridTable", "Contact", new { area = CRMAreaRegistration.AREA_NAME, ContractorId = Model.Id, GridId = "ContractorContacts_" + Model.Id }) 
}